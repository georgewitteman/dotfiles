#compdef merge

_merge() {
  _arguments \
    {-c,--continue}'[finish merge]' \
    '--abort[abort a current merge]' \
    '--no-verify[bypass pre-commit and commit-msg hooks]' \
    '--remote[which remote to merge from (defaults to "origin")]'
}
