#!/usr/bin/env zsh

local cmd="$@"

echo "${fg_bold[blue]}\$ ${(q-)@}$reset_color"
eval "${(q-)@}" >/dev/null 2>&1
if [[ "$status" -ne 0 ]]; then
  echo "${fg_bold[red]}'${(q-)@}' failed! :($reset_color"
else
  echo "${fg_bold[green]}\$ ${(q-)@}$reset_color"
fi
