#!/usr/bin/env zsh

if ! is_clean_repo; then
  echo "${fg[yellow]}Warning:$reset_color You have uncommitted changes. Commit or stash your changes."
  echo_run git status
  return 1
fi

local branches branch
branches=$(
  git \
    --no-pager \
    branch \
    --sort=-committerdate \
    --format="%(if)%(HEAD)%(then)*%(else) %(end) %(refname:short) (%(committerdate:relative))"
) &&
branch=$(
  echo "$branches" |
  fzf --no-multi --nth=1,2 \
    --preview="git show --color=always {1}" --preview-window="70%"
) &&
branch_name=$(echo "$branch" | awk '{print $1}' | sed "s/.* //") &&
if [ "$branch_name" = "*" ]; then
  echo -n "Already on branch $bold_color$(echo "$branch" | awk '{print $2}' | sed 's/.* //')"
  echo "$reset_color"
  return
fi &&
echo_run git checkout $branch_name || return "$status"
git_show --quiet
