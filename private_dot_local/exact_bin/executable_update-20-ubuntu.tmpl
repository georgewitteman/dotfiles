{{- if eq .chezmoi.os "linux" -}}
#!/bin/sh

set -e

if ! command -v apt >/dev/null 2>&1; then
  echo-warn "The 'apt' command is not available. Skipping Ubuntu"
  exit
fi

sudo apt-get install ripgrep neovim
{{ end -}}
