{{ if .is_work -}}
# vim: ft=gitconfig
[user]
	email = {{ .work.email | quote }}
{{- if .work.signingkey }}
	signingKey = {{ .work.signingkey | quote }}
{{- end }}
{{- if (and (lookPath "gpg") (eq (output "sh" "-c" (cat "gpg --list-secret-keys " (quote .work.signingkey) " >/dev/null 2>&1 || echo false")) "")) }}
[commit]
	gpgsign = true
{{- end }}
{{ end -}}
