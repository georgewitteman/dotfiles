if [ -t 0 ] && [ -t 1 ] && [ -f "${HOME}/.cache/fast-syntax-highlighting/fast-syntax-highlighting.plugin.zsh" ]; then
  source "${HOME}/.cache/fast-syntax-highlighting/fast-syntax-highlighting.plugin.zsh"
fi
