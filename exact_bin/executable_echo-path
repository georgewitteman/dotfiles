#/bin/sh

echo_array() {
    len=$#
    num_len="$(( ${#len} + 1 ))"
    num=0
    for arg in $@; do
        printf "%${num_len}d: %s\n" "$num" "$arg"
        num=$(( $num + 1 ))
    done
}

IFS=: echo_array ${1:-${PATH}}
