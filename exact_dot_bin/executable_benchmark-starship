#!/bin/sh

export STARSHIP_PIPE_STATUS=0
export STARSHIP_CMD_STATUS=0
export STARSHIP_CAPTURED_TIME=1652658642533
export STARSHIP_DURATION=1

# echo-run hyperfine "$(which starship)"' prompt --terminal-width="$COLUMNS" --keymap="${KEYMAP:-}" --status="$STARSHIP_CMD_STATUS" --pipestatus="${STARSHIP_PIPE_STATUS[*]}" --cmd-duration="${STARSHIP_DURATION:-}" --jobs="$STARSHIP_JOBS_COUNT"'

echo-run hyperfine --warmup 10 "$(which starship) prompt"
