#!/usr/bin/env zsh

if ! check_in_git_repo; then
  echo "Not in git repository."
  exit 128
fi

echo -n "Getting remote HEAD... "
GIT_HEAD="$(get_remote_head)"
echo $GIT_HEAD

echo_run git checkout $GIT_HEAD
echo_run git fetch --prune --tags
echo_run git merge --ff-only origin/$GIT_HEAD
