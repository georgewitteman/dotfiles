#!/usr/bin/env zsh

local system_type="$(uname -s)"
if [ "$system_type" = "Darwin" ]; then
  echo_run sudo killall -HUP mDNSResponder
else
  echo "Can't flush DNS cache on this operating system"
  return 1
fi
